# メディアディレクトリの配置方法

右上の動画エリアに表示するコンテンツを配置する方法について説明します。

## ディレクトリの配置場所

### 開発環境（開発中）

開発環境では、以下のディレクトリにメディアファイルを配置してください：

```
%APPDATA%\Gido Touch Mini\media\
```

Windowsの場合、通常は以下のパスになります：
```
C:\Users\<ユーザー名>\AppData\Roaming\Gido Touch Mini\media\
```

### 本番環境（ビルド後）

本番環境では、アプリケーションのリソースディレクトリに配置されます：

```
<アプリケーションインストールディレクトリ>\resources\media\
```

## サポートされているメディアタイプ

以下のメディアタイプがサポートされています：

### 動画ファイル
- `.mp4` - MPEG-4動画ファイル

### 画像ファイル
- `.jpg`, `.jpeg` - JPEG画像
- `.png` - PNG画像
- `.svg` - SVG画像
- `.webp` - WebP画像

### ZIPファイル
- `.zip` - ZIPアーカイブ（解凍後に上記のメディアタイプが含まれている場合、自動的に再生されます）

## ファイルの配置方法

### 方法1: 直接ファイルを配置

メディアディレクトリに直接ファイルを配置します：

```
media/
├── video1.mp4
├── video2.mp4
├── image1.jpg
├── image2.png
└── image3.svg
```

### 方法2: ZIPファイルを配置

複数のファイルをZIPアーカイブとして配置することもできます：

```
media/
└── content.zip
```

ZIPファイルは自動的に解凍され、含まれるメディアファイルが再生されます。
解凍先は `media/<zipファイル名（拡張子なし）>/` になります。

## ビルド時にコンテンツをセットする方法

### 手順

1. プロジェクトルートに `media` ディレクトリを作成します：

```bash
mkdir media
```

2. `media` ディレクトリにメディアファイルを配置します：

```
media/
├── video1.mp4
├── video2.mp4
├── image1.jpg
└── content.zip
```

3. ビルドを実行します：

```bash
npm run electron:build
```

ビルド時に、`media` ディレクトリの内容が `resources/media/` にコピーされます。

## 再生の動作

- メディアファイルはファイル名の順序で再生されます
- 動画ファイルは最後まで再生されると、次のファイルに自動的に切り替わります
- 画像ファイルは5秒間表示され、その後次のファイルに切り替わります
- すべてのファイルを再生し終わると、最初のファイルに戻ってループします

## 注意事項

- メディアファイルが見つからない場合、エラーメッセージが表示されます
- ZIPファイルの解凍に失敗した場合、そのZIPファイルはスキップされます
- サポートされていないファイル形式は無視されます
- ファイル名はアルファベット順にソートされます




